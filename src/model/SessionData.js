/**
 * Class representing session data
 */
class SessionData {
    constructor(dayText, sessionLength) {
        this.dayText = dayText
        this.sessionLength = sessionLength
    }
}

export default SessionData
